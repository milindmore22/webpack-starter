/* eslint-disable no-unused-vars, no-unused-expressions */

if ( 'undefined' === typeof window.$ ) {
	window.$ = window.jQuery;
}

'use strict';

$( () => {

	console.log( 'Intialaize Admin Main' );

} );

/* eslint-enable */
